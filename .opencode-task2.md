# Schema Audit Fix — Part 2: Python Importers + Tests

Update Python importers to match the schema changes from Part 1 (event_time, ingestion_source removed from health_entries).

## Files to Modify

### 1. `importers/iron-log/import_ironlog.py`
- Stop writing `event_time` to health_entries (use `timestamp` only)
- Stop writing `ingestion_source` to health_entries (use `source` instead)
- Verify `numeric_value` write to training_logs works (column now exists in schema)

### 2. `importers/health-connect/import_health_connect.py`
- Stop writing `event_time` to health_entries
- Stop writing `ingestion_source` to health_entries

### 3. `importers/health-connect/sync.py`
- Stop writing `event_time` to health_entries
- Stop writing `ingestion_source` to health_entries

### 4. `importers/shared.py`
- Update `upsert_record()` and any shared helpers if they reference `event_time` or `ingestion_source`
- If `event_time` is used as a sync/dedup key, replace with `timestamp`

### 5. Fix all Python tests
- `importers/test_shared.py`
- `importers/test_import_ironlog.py`
- `importers/test_import_health_connect.py`
- `importers/test_sync.py`
- Update test fixtures and expected data to match new column sets

## Verification
Run: `cd /home/lucca/Projects/alexandria && python -m pytest importers/ -v`
Fix any failures.

Then run full suite: `./run-tests.sh`

## Commit
`git add -A && git commit -m "Schema audit: update importers and tests for column changes"`
