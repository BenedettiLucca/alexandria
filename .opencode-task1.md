# Lint + Format Fix — Python

Fix all Python lint issues and format all files.

## Lint Fixes (ruff)

1. **Remove unused imports** in these test files:
   - `importers/test_import_health_connect.py`: remove `pytest`, remove `sha256` from hashlib
   - `importers/test_import_ironlog.py`: remove `pytest`, remove `call` from unittest.mock
   - `importers/test_sync.py`: remove `pytest`, remove `call` from unittest.mock, remove `sha256` from hashlib

2. **Fix bare except** in `importers/health-connect/import_health_connect.py` line 300:
   - Change `except:` to `except Exception:`

## Format
After fixing lint issues, run: `ruff format importers/`

## Verify
Run: `cd /home/lucca/Projects/alexandria && python -m pytest importers/ -v`
Then: `ruff check importers/` should be clean
Then: `ruff format --check importers/` should be clean

## Commit
`git add -A && git commit -m "lint: fix Python unused imports, bare except, and format all files"`
