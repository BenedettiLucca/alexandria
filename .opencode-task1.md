# Schema Audit Fix — Part 1: Schema + Server TypeScript

Apply schema column changes and update all TypeScript files to match.

## Files to Modify

### 1. `schema/schema.sql`:

- **Remove** `event_time TIMESTAMPTZ` column from `health_entries` table (redundant with `timestamp`)
- **Remove** `ingestion_source TEXT` column from `health_entries` table (redundant with `source`)
- **Remove** `dates TEXT[]` column from `memories` table
- **Remove** `decisions JSONB` column from `projects` table
- **Remove** `content_fingerprint TEXT` column from `memories` table
- **Remove** `aliases TEXT[]` column from `entities` table
- **Add** `numeric_value NUMERIC` column to `training_logs` table (near `volume_kg`, `rpe`)
- **Add** indexes on all `user_id` FK columns:
  - `CREATE INDEX idx_memories_user_id ON memories(user_id);`
  - `CREATE INDEX idx_projects_user_id ON projects(user_id);`
  - `CREATE INDEX idx_health_entries_user_id ON health_entries(user_id);`
  - `CREATE INDEX idx_training_logs_user_id ON training_logs(user_id);`
- Update ALL functions (RPCs like `search_health_entries`, `upsert_memory`, etc.) that reference removed columns
- Update ALL indexes that reference removed columns
- Update ALL RLS policies that reference removed columns
- Keep everything else exactly as-is

### 2. `server/index.ts`:

- Replace all `event_time` references on health_entries with `timestamp`
- Replace all `ingestion_source` references with `source`
- Remove any references to `memories.dates`
- Remove any references to `projects.decisions`
- Remove any references to `memories.content_fingerprint`
- Remove any references to `entities.aliases`
- In `health_summary` tool output, include `computed_at` so users know when the summary was generated
- Do NOT change MCP tool names or parameter signatures

### 3. `server/lib.ts`:

- Update `recordToText()` if it references `event_time` or `ingestion_source`
- Update any other pure functions that reference removed columns

### 4. `server/lib.test.ts`:

- Fix any test data objects that include removed columns
- Add a test for `numeric_value` in training_logs if relevant

## Verification
Run: `cd /home/lucca/Projects/alexandria && deno test server/lib.test.ts --allow-all`
Fix any failures before proceeding.

## Commit
`git add -A && git commit -m "Schema audit: remove redundant columns, add missing numeric_value + indexes"`
